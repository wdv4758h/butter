Improve and standardise docstrings and module level docstrings
finish clone() and prctl call
asserts on all applicable input values
double check function arg ordering
review 'high level' interface details and check to see if there is a simplier interface
examples for all code
sphinx documentation
upload sphinx documentation to read the docs and docs.blitz.works
Make close on exec the default for all 'fd' operations to be in line with python 3.4
check function call arg order and ensure they are in sane order
check args and see if sane default args can be provided in most cases
